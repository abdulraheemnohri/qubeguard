<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.qubeguard.app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- VPN Service Permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_VPN" />

    <!-- WebView Permissions -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />

    <application
        android:name=".QubeGuardApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QubeGuard"
        tools:targetApi="31">

        <!-- VPN Service -->
        <service
            android:name=".vpn.VpnServiceImplementation"
            android:description="@string/vpn_service_description"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="vpn|dataSync"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.QubeGuard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Browser Activity -->
        <activity
            android:name=".ui.BrowserActivity"
            android:exported="false"
            android:label="@string/browser_title"
            android:theme="@style/Theme.QubeGuard" />

        <!-- Block Page Activity -->
        <activity
            android:name=".ui.BlockPageActivity"
            android:exported="false"
            android:label="@string/block_page_title"
            android:theme="@style/Theme.QubeGuard" />

        <!-- Settings Activity -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:theme="@style/Theme.QubeGuard" />

        <!-- WorkManager Initializer -->
        <provider
            android:name="androidx.work.impl.background.systemjob.SystemJobInitializer"
            android:authorities="${applicationId}.workmanager-init"
            android:exported="false" />

        <!-- Room Database -->
        <provider
            android:name="androidx.room.RoomDatabaseProvider"
            android:authorities="${applicationId}.room-provider"
            android:exported="false" />
    </application>
</manifest>